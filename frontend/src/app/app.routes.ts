import { Routes } from '@angular/router';
import { ChatbotComponent } from './componenets/chatbot/chatbot.component';

export const routes: Routes = [
    { path: 'chat-bot', component: ChatbotComponent },
];
